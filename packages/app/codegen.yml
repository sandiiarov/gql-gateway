schema: http://localhost:4000/graphql
documents: ./src/**/*.{ts,tsx}
overwrite: true
generates:
  ./types.ts:
    config:
      noNamespaces: true
    plugins:
      - typescript-common
      - typescript-client
