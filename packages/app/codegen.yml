overwrite: true
schema: http://localhost:4000/graphql
documents: ./src/**/*.{ts,tsx}
generates:
  ./generated/graphql.ts:
    config:
      noNamespaces: true
    plugins:
      - typescript-common
      - typescript-client
